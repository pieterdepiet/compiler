class Greeter {
    var greetCount : Int;
    var greetstring : String;
    fun setGreet(to str : String) {
        this.greetstring = str;
    }
    fun greet() {
        prints(this.greetstring);
        this.greetCount += 1;
    }
    fun greet(_ str : String) {
        prints(str);
    }
}

fun start() {
    var g : Greeter = new Greeter();
    var a : String = "Hello world!";
    g.setGreet(to: a);
    g.greet();
    g.greet("Bye!");
}